/**
 * Persistent storage and archival utilities for events and features.
 *
 * <p>Provides abstractions for object storage (MinIO), Parquet writing,
 * and background archival tasks that move events from Redpanda to
 * long-term analytical storage.</p>
 */
package io.muninn.storage;
