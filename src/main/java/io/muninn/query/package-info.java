/**
 * Read-only query API for feature time series and event metadata.
 *
 * <p>Serves data from DuckDB over the Parquet warehouse. No mutations,
 * no side effects, no writes to the event log.</p>
 */
package io.muninn.query;
