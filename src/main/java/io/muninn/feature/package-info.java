/**
 * Feature engine for deterministic computation over market event streams.
 *
 * <p>This package contains the core logic for consuming raw events,
 * applying feature definitions (like VWAP, candles), and emitting
 * computed features to the event log.</p>
 */
package io.muninn.feature;
